import styled from 'styled-components'

export const Block = styled.div`
  p {
    font-size: 12px;
    color: rgb(113, 113, 113);
    margin-bottom: 8px;
  }

  h1 {
    font-size: 36px;
    font-weight: 600;
  }
`
