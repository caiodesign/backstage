import styled from 'styled-components'

export const Image = styled.div`
  position: relative;
`

export const Counter = styled.p`
  position: absolute;
  bottom: 10px;
  color: #fff;
  opacity: 0.9;
  font-size: 12px;
`
